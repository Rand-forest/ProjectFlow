# ProjectFlow

Change Log (v1.0.0 - Initialization)
HTML: Created index.html replicating the ProjectFlow landing page structure (Navbar, Hero, Features, Footer).

CSS: Implemented styles.css using CSS variables for scalable theming and Flexbox for responsive layout.

JS: Created script.js stub for future API integration.

Apps Script (.gs): Built foundational Code.gs utilizing doGet and doPost methods.

Security: Implemented PropertiesService snippet in GAS to safely abstract the Database Spreadsheet ID away from raw source code.

Deploy both environments (push your code to GitHub, and deploy your Apps Script as a Web App accessible to "Anyone"). Once you have done this, we can proceed to mapping out the spreadsheet data structure and building the authentication/login forms.
